# Use the classic node_modules layout. Yarn 4 defaults to Plug'n'Play,
# which vue-tsc / the Vite toolchain don't resolve without extra SDK
# wiring; node-modules keeps tooling friction-free and matches the
# container build.
nodeLinker: node-modules
